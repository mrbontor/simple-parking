{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["name"],
    "properties": {
        "name": {
            "type": "string",
            "minLength": 3,
            "errorMessage": {
                "minLength": "Plate should be greater than 3 character.",
                "type": "Plate must be string"
            }
        },
        "description": {
            "type": ["string", "null"],
            "default": null
        },
        "status": {
            "type": "boolean",
            "enum": [true, false],
            "default": true,
            "errorMessage": {
                "enum": "Please select one of options."
            }
        }
    },
    "additionalProperties": false,
    "errorMessage": {
        "type": "should be an object",
        "required": {
            "name": "Plate is required!"
        }
    }
}
